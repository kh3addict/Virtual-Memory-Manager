  #pragma once

   class TLBentry{// TLB entry

 public:

  int pageNumber = 0;// each TLB entry has a pageNumber
  int frame = 0;// each TLB entry has a frame associated with that pageNumber
    
  

 };